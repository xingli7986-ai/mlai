<!-- 本文档 v1.2 = v1.1 主文档(保留不动) + v1.2 补丁合并;v1.2 增量内容已用 "v1.2 增量(基于 v1.1 + 补丁合并)" 标注。日期 2026-05-02 -->

> ⚠️ 本文档为 v1.2 合并版,由 `MaxLuLu AI 用户流程（User Flow）v1.1.md` 与 `04_UserFlow_用户流程_v1.2_补丁.md` 自动合并产出。原 v1.1 文件保留不动,补丁文件保留作为变更追溯。
> 当前合并策略:v1.1 全文在前 → v1.2 补丁在后;补丁中标注 "替换 v1.1 §X" 的章节以补丁为准。

---

# 第一部分:v1.1 主文档(原文保留)

# MaxLuLu AI 用户流程（User Flow）

> 版本：v1.1 | 日期：2026-05-01 定义每种用户角色的核心操作路径，标注每一步涉及的页面和 API。

---

## 一、消费者 — 参团购买流程（核心路径，占收入 70%）

```
┌─────────────┐
│  用户访问首页  │  页面: /
│  浏览热拼/热门  │  API: GET /api/home
└──────┬──────┘
       │ 点击商品卡片
       ▼
┌─────────────┐
│  印花衣橱    │  页面: /products
│  筛选/排序/翻页│  API: GET /api/designs?sort=&category=&page=
└──────┬──────┘
       │ 点击设计卡片
       ▼
┌─────────────┐
│  设计详情页   │  页面: /products/[id]
│  查看图片/描述 │  API: GET /api/designs/[id]
│  点赞/收藏/评论│  API: POST .../like|favorite|comments
└──────┬──────┘
       │ 点击"参团购买"
       ▼
┌─────────────┐
│  查找或创建拼团│  API: POST /api/group-buys/find-or-create { designId }
│  返回 groupBuyId│  ⚠️ 必须用 POST，不是 GET
└──────┬──────┘
       │ 前端跳转
       ▼
┌─────────────┐
│  参团购买页   │  页面: /group-buy/[id]
│  显示拼团详情  │  API: GET /api/group-buys/[id]（只读）
│  选尺码/填地址 │
│  有邀请码可抵扣 │
└──────┬──────┘
       │ 点击"立即支付"
       ▼
┌─────────────┐         ┌─────────────┐
│  未登录？     │── 是 ──→│  弹出登录框   │
└──────┬──────┘         └──────┬──────┘
       │ 已登录                 │ 登录成功
       ▼                       ▼
┌─────────────┐
│  创建参团订单  │  API: POST /api/group-buys/[id]/orders
│  订单状态:     │  → pending_payment
│  事务内操作:   │  → currentCount++
│              │  → 校验邀请码有效性
│              │  → 计算折扣金额
└──────┬──────┘
       ▼
┌─────────────┐
│  发起支付    │  API: POST /api/payment/create {orderId, kind:"group-buy"}
│  后端查库算金额│  ⚠️ 不接受前端传金额
│  返回支付宝表单│
└──────┬──────┘
       │ 表单自动提交
       ▼
┌─────────────┐
│  支付宝收银台  │  外部页面
└──────┬──────┘
       │
       ├─── 支付成功 ──→ 异步回调 + 同步跳转（见下方）
       ├─── 用户取消 ──→ 订单保持 pending_payment，等待超时关闭
       └─── 支付失败 ──→ 订单保持 pending_payment，用户可重新支付
```

### 支付成功后的回调处理

```
支付宝异步回调（POST /api/payment/alipay/notify）:
├── 解析 form-urlencoded 请求体
├── 验证签名（checkNotifySign）
├── 幂等检查：订单已是 paid → 直接返回 "success"，不重复处理
├── 金额校验：回调金额 vs 数据库订单金额，不一致则记录告警
├── 更新订单：status → paid, paymentId → trade_no
├── 触发 processForProduction（幂等守卫）
└── 返回纯文本 "success"（Content-Type: text/plain）

支付宝同步回调（GET /api/payment/alipay/return）:
├── 从 URL 取 out_trade_no
└── 重定向到 /group-buy/[id]/progress

⚠️ 同步回调不做业务处理，仅重定向。以异步回调为准。
```

---

## 二、消费者 — 个人定制流程（占收入 20%）

```
┌─────────────┐
│  设计详情页   │  页面: /products/[id]
└──────┬──────┘
       │ 点击"定制此款"
       ▼
┌─────────────┐
│  个人定制页   │  页面: /products/[id]/custom
│  选择:        │
│  - 裙型       │
│  - 面料       │  → 联动定价（基础/中端/高端 × 定制价）
│  - 尺码       │  → 标准码或自定义量体
│  - 领型/袖型/裙长│
│  填写收货信息  │
└──────┬──────┘
       │ 点击"立即定制"
       ▼
┌─────────────┐
│  创建订单    │  API: POST /api/designs/[id]/custom-order
│  订单状态:     │  → pending_payment
│  totalAmount: │  → 定制价（分）
└──────┬──────┘
       ▼
┌─────────────┐
│  发起支付    │  API: POST /api/payment/create {orderId, kind:"order"}
│  后端查库算金额│  ⚠️ 不接受前端传金额
│  返回支付宝表单│
└──────┬──────┘
       │ 表单自动提交
       ▼
┌─────────────┐
│  支付宝收银台  │  外部页面
│  → 支付成功   │  异步回调更新 status → paid
│  → 同步跳回   │  重定向到 /my（订单 Tab）
└─────────────┘
```

---

## 三、消费者 — 邀约裂变流程

```
┌─────────────┐
│  已参团用户   │  页面: /group-buy/[id]/progress 或 /my
│  获得邀请码   │
│  分享链接/码   │
└──────┬──────┘
       │ 分享给朋友
       ▼
┌─────────────┐
│  朋友打开链接  │  页面: /invite/[code]
│  展示:        │
│  - 邀请人信息  │  API: GET /api/invitations/[code]
│  - 设计详情   │
│  - ¥30 优惠提示│
│  - "立即参团"按钮│
└──────┬──────┘
       │ 点击"立即参团"
       ▼
┌─────────────┐
│  参团购买页   │  页面: /group-buy/[id]
│  自动填入邀请码 │  下单时抵扣 ¥30
│  → 后续同参团流程│
└─────────────┘

邀请人在朋友下单成功后，也获得 ¥30 优惠（用于下次购买）
```

---

## 四、设计师 — 入驻流程

```
┌─────────────┐
│  消费者导航   │  点击"设计师入驻"
│  或直接访问   │  → /studio/join（公开布局，无侧边栏）
└──────┬──────┘
       ▼
┌─────────────┐
│  入驻页       │  了解平台/分润(10%)/工具/流程/案例
└──────┬──────┘
       │ 点击"立即申请入驻"
       ▼
┌─────────────┐
│  申请表单     │  姓名/手机号/设计经验/作品链接/自我介绍
└──────┬──────┘
       │ 提交
       ▼
┌─────────────┐
│  等待审核     │  API: POST /api/designer/register
│  role: designer_pending│
└──────┬──────┘
       │ 管理员在 /admin/manage 审核
       ▼
┌─────────────┐
│  审核通过     │  role → designer
│  → 引导进入 Studio│  跳转 /studio
│  → 开始使用工具  │
└─────────────┘
```

---

## 五、设计师 — 创作与发布流程

```
┌─────────────┐
│  工具首页     │  页面: /studio
│  选择 AI 工具  │  16 个工具卡片
└──────┬──────┘
       │ 点击工具卡片
       ▼
┌─────────────┐
│  AI 工具页    │  页面: /studio/pattern/* 或 /studio/fashion/*
│  输入 prompt   │
│  选择参数     │
│  点击"生成"   │  API: POST /api/ai-studio/generate
│  查看结果     │  图片存 R2
│  可重复生成   │
│  满意后点"保存"│  保存到 Design（私有）
└──────┬──────┘
       │ 点击"发布到平台"
       ▼
┌─────────────┐
│  发布设计     │  页面: /studio/publish
│  4 步流程:    │
│  1. 选择设计   │  从已保存的 Design 中选
│  2. 填写信息   │  标题/描述/面料/类别
│  3. 确认定价   │  系统自动定价，显示分润预估
│  4. 提交审核   │  API: POST /api/designs/publish（待实现）
└──────┬──────┘
       │ 管理员审核通过
       ▼
┌─────────────┐
│  作品上架     │  出现在 /products 印花衣橱
│  消费者可购买  │
└─────────────┘
```

---

## 六、设计师 — 收入与提现流程

```
┌─────────────┐
│  消费者购买   │  参团或定制设计师的作品
│  确认收货     │
└──────┬──────┘
       │ 7 天后
       ▼
┌─────────────┐
│  分润结算     │  订单金额 × 10% 计入设计师可提现余额
└──────┬──────┘
       ▼
┌─────────────┐
│  设计师中心   │  页面: /studio/dashboard
│  查看收入统计  │  API: GET /api/designer/dashboard
│  查看可提现金额│
└──────┬──────┘
       │ 余额 ≥ ¥100 时
       │ 点击"申请提现"
       ▼
┌─────────────┐
│  提现申请     │  API: POST /api/designer/withdrawal
│  输入金额     │
│  确认支付宝账号│
└──────┬──────┘
       │ 管理员在 /admin/manage 处理
       ▼
┌─────────────┐
│  提现完成     │  状态: pending → processing → completed
│  支付宝到账   │
└─────────────┘
```

---

## 七、管理员 — 日常运营流程

```
管理员登录 → /admin/manage

Tab 1: 设计审核
├── 查看待审核设计列表
├── 点击"通过" → API: POST /api/admin/designs {action: approve}
└── 点击"拒绝" → API: POST /api/admin/designs {action: reject}

Tab 2: 拼团管理
├── 查看所有拼团状态
├── 手动 fulfill（满团发货）
├── 手动 expire（提前结束）
└── 手动 extend（延长时间）

Tab 3: 提现管理
├── 查看待处理提现
├── 点击"通过" → 状态变 processing → 手动转账 → 标记 completed
└── 点击"拒绝" → 状态变 rejected
```

---

## 八、登录流程（所有需要登录的操作共用）

```
┌─────────────┐
│  触发登录     │  点赞/收藏/下单/进个人中心/进 Studio 工具
└──────┬──────┘
       ▼
┌─────────────┐
│  弹出登录框   │  组件（非独立页面）
│  输入手机号   │
│  点击"获取验证码"│  API: POST /api/auth/send-code
│              │  60 秒倒计时，5 分钟过期
│  输入 6 位验证码│
│  点击"登录"   │  API: POST /api/auth/verify
└──────┬──────┘
       │ 验证成功
       ▼
┌─────────────┐
│  登录成功     │  next-auth session 建立
│  返回原页面   │  继续之前的操作
└─────────────┘

首次登录自动注册（手机号即账号）
```

---

## 九、订单状态机

### 9.1 个人定制订单状态

```
pending_payment ──支付成功──→ paid ──进入生产──→ in_production ──发货──→ shipped ──确认收货──→ completed
      │                        │
      │                        └──用户申请退款──→ refund_pending ──退款完成──→ refunded
      │
      └──30分钟超时──→ closed
```

|状态|中文|触发条件|用户可见|
|---|---|---|---|
|pending_payment|待支付|创建订单时|显示"去支付"按钮|
|paid|已支付|支付宝异步回调确认|显示"等待生产"|
|in_production|生产中|管理员标记|显示"生产中"|
|shipped|已发货|管理员填写物流单号|显示物流信息|
|completed|已完成|用户确认收货 或 发货后15天自动确认|显示"已完成"|
|refund_pending|退款中|用户申请退款（paid 状态下）|显示"退款处理中"|
|refunded|已退款|管理员确认退款|显示"已退款"|
|closed|已关闭|30分钟未支付自动关闭|显示"订单已关闭"|

### 9.2 参团订单状态

```
pending_payment ──支付成功──→ paid ──进入拼团──→ grouping
      │                                          │
      │                              ┌────────────┤
      │                              │            │
      │                       满30人成团      72h未满
      │                              │            │
      │                              ▼            ▼
      │                        group_success  group_failed
      │                              │            │
      │                        进入生产        自动退款
      │                              │            │
      │                              ▼            ▼
      │                      in_production    refunded
      │                              │
      │                        发货→完成（同定制）
      │
      └──30分钟超时──→ closed
```

|状态|中文|触发条件|
|---|---|---|
|pending_payment|待支付|创建参团订单时|
|paid|已支付|支付宝回调确认|
|grouping|拼团中|支付成功后自动|
|group_success|拼团成功|满 30 人|
|group_failed|拼团失败|72h 超时未满|
|in_production|生产中|拼团成功后进入生产|
|shipped|已发货|管理员操作|
|completed|已完成|确认收货|
|refunded|已退款|拼团失败自动退款|
|closed|已关闭|30分钟未支付|

### 9.3 拼团状态（GroupBuy 维度，非订单维度）

|状态|中文|触发条件|
|---|---|---|
|active|进行中|创建拼团时|
|fulfilled|已满团|currentCount 达到 30|
|expired|已过期|72h 未满，Cron 标记|
|cancelled|已取消|管理员手动取消|

---

## 十、支付规则与异常处理

### 10.1 核心规则

|规则|说明|
|---|---|
|金额来源|后端从数据库订单读取 totalAmount，不接受前端传金额|
|金额单位|存储用分（Int），传给支付宝时 /100 转元 toFixed(2)|
|支付超时|timeout_express: 30m，30 分钟未支付自动关闭|
|订单关闭|closed 状态的订单不可再支付|

### 10.2 异步回调（notify）规则

|规则|说明|
|---|---|
|幂等处理|同一个 out_trade_no，如果订单已是 paid/后续状态，直接返回 "success"，不重复入账|
|签名验证|必须调用 sdk.checkNotifySign(params)，验签失败返回 "fail"|
|金额校验|回调中的 total_amount 与数据库 order.totalAmount/100 对比，不一致记录告警日志|
|返回格式|纯文本 "success"，Content-Type: text/plain，不是 JSON|
|重复推送|支付宝最多推送 24 小时，必须每次都返回 "success" 才停止|

### 10.3 异常场景处理

|异常场景|处理方式|
|---|---|
|用户打开支付宝后取消|订单保持 pending_payment，不做任何处理|
|支付成功但同步回调失败|以异步回调为准，同步回调仅做重定向|
|支付宝重复发送 notify|幂等处理，已 paid 直接返回 "success"|
|30 分钟超时未支付|支付宝自动关闭交易，订单状态 → closed|
|前端传入篡改金额|后端不读前端金额，只从数据库取|
|拼团失败需退款|Cron 触发，调用支付宝退款 API，订单状态 → refunded|

---

# 第二部分:v1.2 增量(基于 v1.1 + 补丁合并)

# MaxLuLu AI 用户流程 User Flow v1.2 补丁

> 基于 v1.1 增量补充。
> 版本:v1.2 | 日期:2026-05-02

---

## 流程 A:消费者创作 + 分享 + 商业化

```
登录 → 顶栏点"我的设计工作室" → /my-studio

进入工作室,选 4 种创作方式之一:
├─ 基于已有款式 → 进入款式库选择 → 携带参数进入创作
├─ 从零设计 → 空白画布开始
├─ 基于个人需求 → 填写场景/性格/身材问卷 → AI 推荐花型
└─ 上传图片素材 → 上传图 → AI 基于图生成印花

创作过程(用 4 个 AI 工具:印花生成 / 四方连续 / 图案上身 / AI 试衣)
        ↓
作品保存到"我的作品"
        ↓
选择 3 个出口之一:
├─ 出口 1:单独下单定制
│    → /my-studio 内点"下单定制"
│    → 选尺码/面料/数量
│    → 填地址
│    → 支付宝支付
│    → 进入个人订单(走 v1.1 订单状态机)
│
├─ 出口 2:发起团购
│    → 点"发起团购"
│    → 提交"上架审核"申请
│    → 管理员审核(标 P1 必做)
│    → 通过 → 创建团购,进入印花衣橱
│    → 团购规则同 v1.1(30 件/72h)
│
└─ 出口 3:分享到灵感广场
     → 点"分享"
     → 选择 prompt 公开方式(免费/付费/不公开)
     → AI 自动审核
     ├─ 通过 → 立即出现在 /inspiration
     └─ 不通过 → 退回修改 + 提示原因
```

---

## 流程 B:游客/消费者从灵感广场流转

```
访问 /inspiration → 浏览作品(无需登录)

发现喜欢的作品 → 点击进入 /inspiration/[id]
       ↓
四种动作:

动作 1:点赞/收藏/评论
       → 未登录:弹登录框
       → 已登录:直接执行
       
动作 2:基于这个改造
       → 未登录:弹登录框
       → 已登录:跳 /my-studio?from=inspirationId
       → 工作室携带原作品参数,用户可继续修改创作
       
动作 3:发起团购请求
       → 未登录:弹登录框
       → 已登录:发送通知给作者
       → 作者同意 → 走"上架审核流程" → 通过后开团
       → 作者拒绝 → 通知请求者
       
动作 4:解锁创作详情(prompt + 参数)
       → 未登录:弹登录框
       → 已登录 + 作品免费公开:直接显示
       → 已登录 + 付费解锁:
            → 选择支付方式(现金 / 积分)
            → 支付成功 → 显示 prompt + 参数
            → 解锁记录入库
       → 已登录 + 不公开:显示"作者未公开此作品创作详情"
```

---

## 流程 C:用户升级为设计师

```
普通用户持续创作和分享
       ↓
系统每日扫描用户作品累计数据
       ↓
检测到达到任一阈值:
├─ 累计赞数 ≥ 500
├─ 累计收藏数 ≥ 200
└─ 累计被购/团购件数 ≥ 30
       ↓
创建 UpgradeInvitation 记录(30 天有效期)
       ↓
通知用户:站内消息 + 短信
       ↓
用户在 30 天内响应:
├─ 接受 → 跳 /studio/join?invitation=xxx
│         → 填写补充信息、签协议
│         → 提交审核
│         → 管理员审核(7 工作日)
│         → 通过 → 升级为认证设计师
│              → 历史作品保留 + 自动加金色描边
│              → 创作次数升级为 20/天
│              → 分润切换到 10-20% 阶梯
│         → 拒绝 → 通知用户原因 + 建议改进
│
├─ 拒绝 → 邀请关闭(可主动重新申请)
│
└─ 30 天未响应 → 邀请过期(可主动重新申请)
```

---

## 流程 D:上架审核流程(用户作品想成商品)

```
用户在 /my-studio 选某作品 → 点"申请上架"
       ↓
填写商品信息:
├─ 设定面料档位(基础/中端/高端)→ 系统自动算价格区间
├─ 选择销售方式(团购 / 单独定制 / 两者皆可)
└─ 提交审核
       ↓
进入管理员审核队列
       ↓
管理员审核(参考 PRD §7.1):
├─ 通过 → 创建商品记录
│         ├─ 团购:进入印花衣橱 + 启动 72h 倒计时
│         └─ 单独定制:进入印花衣橱 + 长期可定制
│         → 通知作者
│
└─ 拒绝 → 退回 + 说明原因 + 通知作者
```

---

## 异常处理新增

### prompt 解锁支付失败

- 支付未成功 → 不显示 prompt + 退款 + 重试

### 上架审核被拒

- 通知作者,允许修改后重新提交,无次数限制

### 自动审核误杀

- 用户可申诉
- 管理员人工复核

### 升级邀请响应超时

- 30 天后自动过期,作品历史保留
- 用户随时可主动通过 /studio/join 申请

---

## v1.1 订单状态机不变

定制订单和参团订单的状态机沿用 v1.1 §9 章。
新流程产生的订单同样走该状态机,不区分作品来源(用户/设计师)。
